---
inclusion: fileMatch
fileMatchPattern: "**/*.{md,mdx}"
---

# Markdown Tables

- Always use spaced separator rows: `| --- |` not `|---|`
- Align content left (default) unless numbers warrant right-alignment
- Keep tables readable in source — pad cells with spaces
